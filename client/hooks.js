AutoForm.hooks({
  insertPostForm: {
    formToDoc: function(doc, ss, formId) {
          // doc.job = Router.current().params._id
          // return doc

          // get id of event
          // add it to the doc
          // return doc

      },

    },


    insertCommentForm: {
    formToDoc: function(doc, ss, formId) {
          // doc.post = Router.current().params._id
          // return doc

          // get id of post
          // add it to the doc
          // return doc
      },

    }
 })